{
  "type": "object",
  "properties": {
    "state": { "type": "string" },
    "errorMessage": { "type": "string" },
    "downloadUrls": {
      "type": "array",
      "items": { "type": "string" }
    }
  },
  "required": ["state"]
}
Headers: Authorization: @{concat('Bearer ', body('Parse_Token')?['access_token'])}
