.
concat(
    '<div>',
    '<h2>', if(empty(items('Apply_to_each')?['Title']), '', items('Apply_to_each')?['Title']), '</h2>',
    if(empty(items('Apply_to_each')?['BannerImageUrl']), '', concat('<img src="', items('Apply_to_each')?['BannerImageUrl'], '" style="width:100%;max-height:300px;object-fit:cover;"/>')),
    if(empty(items('Apply_to_each')?['Description']), '', concat('<p><em>', items('Apply_to_each')?['Description'], '</em></p>')),
    if(empty(items('Apply_to_each')?['CanvasContent1']), '', items('Apply_to_each')?['CanvasContent1']),
    if(empty(items('Apply_to_each')?['LayoutWebpartsContent']), '', concat('<div><pre>', items('Apply_to_each')?['LayoutWebpartsContent'], '</pre></div>')),
    '</div>'
)
